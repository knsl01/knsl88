# Archive

Legacy prototypes and numbered duplicates (`main99`, `KNSLLegalIntelligence77`, Tailwind `screen-*` stack, etc.).

**Not used by production entry** (`src/main.jsx`). Kept for reference; safe to delete after review.
